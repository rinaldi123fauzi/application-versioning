<div class="row wrapper border-bottom white-bg page-heading">
  <div class="col-lg-10">
    <h2>Form priority</h2>
    <ol class="breadcrumb">
      <li>
        <a href="">Home</a>
      </li>
      <li>
        <a href="<%= priorities_url %>">List</a>
      </li>
      <li class="active">
        <strong>Form</strong>
      </li>
    </ol>
  </div>
</div>
<br>
<div id="content">
  <div class="page-header">
    <%= link_to new_priority_path, class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    New Priority
    <% end %>
    <!--    <h1>Listing </h1>-->
  </div>

  <div class="table-responsive">
    <table class="table table-striped table-bordered table-hover dataTables-example">
      <thead>
      <tr>
        <th>No</th>
                  <th>Nama</th>
                  <th>Deskripsi</th>
                <th></th>
      </tr>
      </thead>

      <tbody>
      <% @no = 0%>
      <%= content_tag_for(:tr, @priorities) do |priority| %>
        <td><%= @no += 1 %></td>
                  <td><%= priority.nama %></td>
                  <td><%= priority.deskripsi %></td>
                <td>
                    <%= link_to '', priority_path(priority.id), class: "fa fa-eye btn btn-xs btn-info" %>
                    <%= link_to '', edit_priority_path(priority), class: " fa fa-pencil btn btn-xs btn-warning" %>
                </td>
        <%# <td><%= link_to '', priority, method: :delete, class: "fa fa-trash btn btn-xs btn-danger", data: { confirm: 'Are you sure?' } %>
      <% end %>
      </tbody>
    </table>
  </div>
</div>

<% content_for :javascript do %>

  <script type="text/javascript">
    $(function() {

        $('.dataTables-example').dataTable({
            "dom": 'T<"clear">lfrtip',
            "tableTools": {
                "sSwfPath": "../assets/dataTables/swf/copy_csv_xls_pdf.swf"
            }
        });
    });
  </script>

<% end %>
